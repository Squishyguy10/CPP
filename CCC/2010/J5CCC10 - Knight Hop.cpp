#include <bits/stdc++.h>
using namespace std;

const int N = 9;

bool board[9][9];

int hop() {
    if(x>0 && x<=N && y>0 && y<=N) {
        if()
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int startX, startY, endX, endY;
    int moves = 0;
    cin >> startX >> startY >> endX >> endY;
    

}