#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
    int youngest, middle, oldest, difference;

    cin >> youngest >> middle;

    difference = middle - youngest;
    oldest = middle + difference;
    
    cout << oldest << endl;

    // Method 1, C++, Python, etc.
    int youngest_person;

    // Method 2, Javascript
    int youngestPerson;

    return 0;
}