#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
    int input;
    cin >> input;
    int side_length = sqrt(input);
    
    cout << "The largest square has side length " << side_length << "." << endl;
    return 0;
}