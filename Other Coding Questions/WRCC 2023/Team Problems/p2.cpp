#include <bits/stdc++.h> 
using namespace std;
    
#define int long long
#define f first
#define s second
#define pii pair<int, int>
#define pb push_back
    
int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int n, brad, prad, vx, vy;
    cin >> n >> brad >> prad >> vx >> vy;

    pii pins[n];
    for(int i = 0; i < n; i++) {
        cin >> pins[i].f >> pins[i].s;
    }
    
    

    
    
    return 0;
}