#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int loops; cin >> loops;
    int sums[loops-1];
    int nums[loops] = {};
    int first, second; cin >> first >> second;
    int outp = 0;

    


    
    cout << min(first, second)-1 << endl;
    

    return 0;
}